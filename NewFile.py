# Terminal display path: /workspaces/daytwostuff

# %% 
import pandas as pd


# %%
data = pd.read_csv("products-100.csv")


# %% [markdown]
# What is your terminal display "path"? (type/paste as text into the .py file) 
# The terminal display 'path' is /workspaces/daytwostuff

# Should you include the environment in your repo or not?  
# You shouldnt include the environment in your repo because the requirments.txt file will tell the user which modules they need to install. 

# Now what is your terminal display "path"? Is it different? 
# The Terminal display "path" now has the venv in front indicating that the virtual environment is active 

# Find the extension in the extension menu. What do you notice about the extension menu? 
# The extension menu has multiple tabs one for local and the other for Virtual Codespace. 

# what are three useful elements of Data Wrangler? 
# 3 useful elements of Data Wrangler are one click transformations, Automatic pandas code export, and Flash Fill. 

# Why do we use a requirements.txt file?
# We use the requirements.txt file so easily allow users to install on the dependencies and their versions really easily. They can use a simple terminal command. 










